%ul
  - @project.uploads.each do |upload|
    %li.image
      = link_to(image_tag(upload.data.url(:small)), upload.data.url(:large), :class => 'group', :rel => 'group', :title => "#{@project.name} project gallery")
.clear